/**
 * Các hàm được sử dụng để thao tác dữ liệu từ tất cả các model của page
*/

// import { Sequelize, Transaction } from "sequelize";
// import { utilsbox } from "../libs.ts";

// // Hàm thêm tất cả dữ liệu page nhận được từ Crawler (ở kiểu PageData sang cấu trúc entities) vào database
// export function addPages(pages: utilsbox.PageData) {

// }